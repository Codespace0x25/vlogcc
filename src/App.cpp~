#include <iostream>
#include "App.hpp"

namespace App {
  App::App(std::string name, std::string appVersion)
    : appname(name), appVersion(appVersion) {
    std::cout << name<< " " <<appVersion<< "\n";
  }

  App::~App() {
  }
}
