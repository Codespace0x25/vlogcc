#pragma once
#include <string>

namespace App {
  class App {
    public:
    App(std::string name, std::string appVersion);
    ~App();

    private:
    std::string appname;
    std::string appVersion;
  };
}
